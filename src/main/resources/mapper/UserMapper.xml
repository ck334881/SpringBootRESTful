<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mobin.dao.UserMapper">
    <select id="createUser" parameterType="user">
        INSERT INTO USERAPI(USERID,USERNAME,PASSWORD,SECRETKEY) VALUES(#{userID},#{userName},#{passWord},#{secretKey})
    </select>
</mapper>